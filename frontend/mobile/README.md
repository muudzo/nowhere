# Mobile frontend (placeholder)

This folder will contain the mobile client (React Native / Expo) code. The mobile path is designed to be stateless: it holds a device token, and all ephemeral interactions go to Redis via the API.
